import 'package:flutter/material.dart';

/// MyCapiz brand colors inspired by the Capiz provincial seal
/// Deep blue for trust and stability, gold for warmth and service
class AppColors {
  // Primary colors from Capiz seal
  static const Color capizBlue = Color(0xFF1B4B7F); // Deep blue
  static const Color capizGold = Color(0xFFD4A13D); // Warm gold

  // Supporting colors
  static const Color backgroundLight = Color(0xFFF8F9FA); // Soft white
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textHint = Color(0xFF999999);

  // Status colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFE57373);
  static const Color info = Color(0xFF42A5F5);

  // Neutral grays
  static const Color gray50 = Color(0xFFFAFAFA);
  static const Color gray100 = Color(0xFFF5F5F5);
  static const Color gray200 = Color(0xFFEEEEEE);
  static const Color gray300 = Color(0xFFE0E0E0);
  static const Color gray400 = Color(0xFFBDBDBD);

  // Shadows and overlays
  static const Color shadowLight = Color(0x0A000000);
  static const Color shadowMedium = Color(0x14000000);
  static const Color overlay = Color(0x80000000);
}
